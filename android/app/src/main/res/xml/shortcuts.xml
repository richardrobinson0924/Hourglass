<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:shortcutId="add"
        android:enabled="true"
        android:icon="@drawable/ic_add2"
        android:shortcutShortLabel="@string/add_event"
        android:shortcutLongLabel="@string/add_event">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetPackage="com.richardrobinson.countdown2"
            android:targetClass="com.richardrobinson.countdown2.MainActivity" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.event" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>